// O que são Arrays
//Array é uma estrutura de dados que armazena vários valores em uma única variável, organizados por índices.
//A solução que vc tem para adicionar vários valores, vários values a uma variável.
const friends = ['Miguel','Marcos','Ana'];
const num = [1,2,3,4, true];
console.log(friends)
console.log(num)