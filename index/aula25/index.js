// Operadores de Atribuição

// =   // Atribui um valor à variável.
// +=  // Soma e atribui (x = x + valor).
// -=  // Subtrai e atribui (x = x - valor).
// *=  // Multiplica e atribui (x = x * valor).
// /=  // Divide e atribui (x = x / valor).
// %=  // Calcula o resto e atribui (x = x % valor).
// **= // Eleva à potência e atribui (x = x ** valor).


let num1 = 3;
num1 += 20;
num1 *= 20;
num1 -= 20;
num1 /= 20;

console.log(num1);