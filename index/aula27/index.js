// Operadores de Igualdade

// ==  // Verifica se os valores são iguais, ignorando o tipo.
// === // Verifica se os valores e tipos são exatamente iguais.
// !=  // Verifica se os valores são diferentes, ignorando o tipo.
// !== // Verifica se os valores e tipos são estritamente diferentes.   

let num1 = '1';
let num2 = 1;

console.log(num1 === num2);
console.log(num1 == num2);